from django.urls import path
from . import views

# App-level URL configuration.
#
# This file is included by project URLs using:
#   path('', include('app.urls'))
#
# Keep this empty if all routing is handled in djangoproject/views.py.
# Add routes here when you want feature-specific endpoints owned by the app.

urlpatterns = [
	# Example routes (currently disabled):
	# path('about/', views.about, name='about'),
	# path('students/', views.student_list, name='student_list'),
]